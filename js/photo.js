'use strict';

(function() {
  /**
   * Конструктор объекта Photo
   * @param {Element} photo
   * @constructor
   * */
  function Photo(photo) {
    this.element = photo;
  }

  module.exports = Photo;
})();
